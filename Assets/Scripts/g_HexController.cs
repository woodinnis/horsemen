using UnityEngine;
using System.Collections;

public class g_HexController : MonoBehaviour
{
	/*
	 * 
	 * This class can and should be a part of m_GridManager.cs
	 * It will be systematically transitioned there over the next few days 
	 * 
	 * g_HexController.cs will be kept and possibly repurposed in some way
	 *  
	 * 
	public Transform occupantTransform;
	public string occupantName;

	public bool isEnemy; // My child is an enemy
	public bool isAlly; // My child is an ally
	public bool isEmpty; // I do not have a child

	public GameObject child; // If I do have a child, this is who it is

	private 

	void Start()
	{
		isEnemy = false;
		isAlly = false;
		isEmpty = true;
	}

	void Update()
	{
		if(Input.GetMouseButtonUp)
		{
			m_GridManager(
		}
	}
	*/
}